<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <parent>
        <groupId>com.funny</groupId>
        <artifactId>moments</artifactId>
        <version>${revision}</version>
    </parent>
    <modelVersion>4.0.0</modelVersion>
    <packaging>jar</packaging>
    <artifactId>moments-service</artifactId>

    <dependencies>
        <dependency>
            <groupId>com.funny</groupId>
            <artifactId>moments-dao</artifactId>
        </dependency>
        <dependency>
            <groupId>com.funny</groupId>
            <artifactId>funny-springboot-starter-service</artifactId>
        </dependency>
        <!-- 可靠性基座（funny-base）：消息/任务/操作日志，业务项目按演示参考使用 -->
        <dependency>
            <groupId>com.funny</groupId>
            <artifactId>base-mq</artifactId>
        </dependency>
        <dependency>
            <groupId>com.funny</groupId>
            <artifactId>base-task</artifactId>
        </dependency>
        <dependency>
            <groupId>com.funny</groupId>
            <artifactId>base-operate-log</artifactId>
        </dependency>
        <dependency>
            <groupId>com.funny</groupId>
            <artifactId>base-data</artifactId>
        </dependency>
        <dependency>
            <groupId>com.funny</groupId>
            <artifactId>funny-springboot-starter-file-cos</artifactId>
        </dependency>
        <!-- C 端签名认证 + 字段解密（@SignVerify / CryptoJsAesDecrypt），使用形态见 funny-bizdev-example LeadsController -->
        <dependency>
            <groupId>com.funny</groupId>
            <artifactId>funny-springboot-starter-sign</artifactId>
        </dependency>
    </dependencies>

</project>
