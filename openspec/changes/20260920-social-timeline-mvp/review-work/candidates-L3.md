# finder=L3 全变更（约定比对，基准=.claude/rules 两份规范；lessons 三文档为空模板）
```json
[
  {"file":"moments-web/src/main/java/com/funny/moments/web/controller/social/PostController.java","line":47,"lens":"L3","severity":"HIGH","claim":"全部 C 端写入接口（发帖/删帖/标签 CRUD/绑定解绑/图片上传）未接入项目已有签名认证，违反 java-guide §2.1 敏感接口【必须】apiKey+timestamp+sign 及 §9 权限校验——项目已装配 SignVerifyInterceptor（WebConfig）但仅拦存量 /leads/**，/api/** 新端点无签名无登录态，身份仅靠明文 viewerId/Cookie 指定可任意伪造","evidence":{"trigger":"任意调用方携带 ?viewerId={他人ID}（CurrentUserResolver 显式参数优先，无身份凭证校验）直接 POST /api/posts 或 DELETE /api/posts/{postId}","failure":"水平越权：冒充任意用户发帖/删帖/打标签；归属校验（NOT_POST_AUTHOR）只防改别人数据防不了冒充本人。注：api.md/design 定稿为 demo 形态（权限列否），与公司级规范冲突，豁免依据需人工确认"}},
  {"file":"moments-web/src/main/java/com/funny/moments/web/controller/social/MockDataController.java","line":37,"lens":"L3","severity":"HIGH","claim":"POST /api/mock-data 支持 clear=true 软删清空全部 8 张表业务数据，无任何鉴权/签名/二次确认服务端校验，违反 java-guide §9 敏感操作前校验用户角色与 §2.1 签名要求；且单请求可触发 10 万用户级长时批量写（MAX_USER_COUNT=100000）","evidence":{"trigger":"未认证请求 POST /api/mock-data body 含 {clear:true,userCount:100000}","failure":"一次匿名调用即清空共享 dev 环境全部数据并长时间占用 DB；前端二次确认（T110）仅页面层，直接 curl 绕过"}},
  {"file":"moments-service/src/main/java/com/funny/moments/service/social/impl/FriendTagServiceImpl.java","line":97,"lens":"L3","severity":"MEDIUM","claim":"createTag(L97)/updateTag(L117)/bindUser(L172)/unbindUser(L182) DB 单 DML 提交成功后裸调用 evictAll()（Redis INCR）无 C12 异常兜底——同变更 FriendTagCacheManager.getTagIds() 全 catch 降级、MockDataServiceImpl.evictCaches() 也 try-catch，唯四处无防护","evidence":{"trigger":"bindUser insert 已自动提交后 Redis 瞬时不可用使 evictAll() incr 抛连接异常","failure":"接口返回 5xx 用户重试但 DB 已生效；版本号未 INCR 旧缓存 TTL 1h 内 canView tagHit 仍用旧标签集合——违反 java-guide §6.4 与变更内 C12 弱依赖定稿"}},
  {"file":"moments-dao/src/main/resources/mapper/PostMapper.xml","line":31,"lens":"L3","severity":"MEDIUM","claim":"selectUserPosts(L31) 硬编码 status=1、selectFeedPage(L48/L53) 硬编码 status=1 与 visibility_type!=2 为裸魔法数字，违反 java-guide §6.2 与 mysql-guide §1——已定义 PostStatusEnum(1=NORMAL)/VisibilityTypeEnum(2=PRIVATE) 未在 SQL 消费且无值含义注释","evidence":{"trigger":"后续扩展 post.status 值域或调整私密帖编码时仅改枚举遗漏 XML 字面量","failure":"Feed 与用户帖子列表过滤口径与新枚举静默分叉（如审核中帖被展示、私密帖泄露进候选集），grep 枚举名找不到引用点"}}
]
```
note: 4 条无截断；已排查未报：软删全覆盖、事务边界、事务内禁 Redis、INSERT 查重、distinct 防唯一键、DO 手写 setter、预编译正则、批量<=500 均合规；DemoSignConfig 硬编码密钥为存量 pre-existing；friendship uniq 不含 is_del 在当前 MVP 功能集（无删好友端点）不可触发未列。
