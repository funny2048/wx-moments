# Review Pack — 20260920-social-timeline-mvp（阶段五用户评审 gate）

> ⚠️ **决策模式**：用户 2026-09-20 夜间下达目标后休息，授权 AI 自主推进（"明天早上我来检查工作结果"）。本 pack 由主 agent 依据各产物结论章节提炼；**全部决策项由 AI 代行确认（逐项附依据），待用户晨间追认**。任何否决项按 WORKFLOW 回退规则处理。

## 一、评审产物索引

| 产物 | 路径 | 状态 |
|------|------|------|
| 需求基线 | prd.md（原始 PRD + 澄清补充 C1-C17） | 冻结候选 |
| 领域匹配 | domain.md | 已确认（AI 代行） |
| 链路文档 | pipeline.md（降级模式，绿地推导） | 已确认 |
| 需求探索 | explore.md（六问全答 + D1-D7 + 风险 R1-R11） | 冻结候选 |
| 技术设计 | design.md（§1-§14，A1-A11 决策） | 冻结候选 |
| API 规格 | api.md（17 接口，16 错误码） | 冻结候选 |
| 任务清单 | tasks.md（17 task / 9 并行组） | 冻结候选 |
| DDL | sql.md（8 表，兼容 MySQL 5.7.44/8.0） | 冻结候选 |
| 测试用例 | test-cases.md（143 条 + 页面级映射） | 冻结候选 |
| 设计审查 | design-review-1(FAIL 3) → -2(FAIL 2) → -3(FAIL 1) → -4(PASS) | 通过 |
| 用例审查 | test-case-review-1(FAIL 4) → -2(PASS) | 通过 |

## 二、关键决策摘要（AI 代行确认项）

| # | 决策点 | 结论 | 依据 |
|---|--------|------|------|
| 1 | 范围 = PRD 全量两期 | 确认 | 目标指令"完成这个需求"；C1 |
| 2 | 前端原生 HTML/JS/CSS 同源托管 | 确认 | C2：无构建链最稳，页面级测试直接可跑 |
| 3 | 当前用户 = viewerId 参数 > Cookie mockUserId | 确认 | A3：无登录体系下的最小可行；越权面已在 §7.3 留痕（实验室定位可接受） |
| 4 | Feed 双锚点算法（页末锚点/扫描进度锚点） | 确认 | design §5.4 四分支走查无矛盾；空页组合显式合法化（review-1 B1 修复后） |
| 5 | 越界策略：缺省默认值，显式越界 1001（mock 1040） | 确认 | 四文档统一（review-1 B2 → review-4 验证闭环） |
| 6 | 三类异常承载（TypeMismatch/HttpMessageNotReadable → 1001；BizException 按码） | 确认 | review-2 NB1 + 裁决追加，advice 层统一 |
| 7 | bindUser 校验顺序 1002 先于 1007 | 确认 | Stage 4 裁决 2（存在性先于关系） |
| 8 | imageUrls 前缀契约 ^/images/[A-Za-z0-9.\_-]+$ 非法 1001 | 确认 | Stage 4 裁决 1（design 真源对齐 api） |
| 9 | DDL 兼容 MySQL 5.7.44（本地 dev 实际）+ 8.0（PRD 目标） | 确认 | 环境实测（brew mysql@5.7 唯一实例）；utf8mb4_general_ci |
| 10 | Redis 弱依赖：好友集合/标签缓存，全局版本号失效，异常回源 DB | 确认 | A4/C12；事务提交后操作 Redis |
| 11 | 删帖/删标签非幂等（1010/1004），绑定查重幂等（1008） | 确认 | review-1 建议 ① 定稿 |
| 12 | mock clear=true 逐表独立小事务 + INCR 失效 | 确认 | R11 防长事务 |
| 13 | 不做上传频控（拒绝 review-1 建议 ⑫） | 确认 | 拒绝理由留痕 design §7.3；生产化前需补（§12 遗留） |
| 14 | 用例 143 条冻结（入口 17/17、错误码 16/16、幂等 9/9、隐私矩阵三路径统一口径） | 确认 | test-case-review-2 PASS |

## 三、两轮对抗结论

**设计（4 轮）**：R1 三阻塞（Feed 空页死循环/越界策略矛盾/viewerId 校验缺失）→ R2 两阻塞回归（1001 无承载/T020 依赖缺口）→ R3 一阻塞（T020 残留，熔断后 AI 代行裁决方案①前移 FriendTagMapper）→ R4 PASS（裁决 4/4 落地，回归 5 面干净）。历史 27 条（6 阻塞+21 建议）全部闭环或留痕拒绝。
**用例（2 轮）**：R1 四阻塞（隐私投影矛盾/双锚点数据量/校验顺序/声明失实）→ R2 PASS（13/13 闭环）。5 建议已处置（4 用例侧修补 + 1 api.md §1.6 勘误）。

## 四、冻结基线清单（文件级）

- design.md / api.md / tasks.md / sql.md / test-cases.md（含 2 项冻结前修补：用例侧 4 建议 + api §1.6 1011 行勘误——修补完成即冻结）
- 后续（阶段六循环/阶段七双评）发现的语义缺口必须回阶段四增补 → 重过对抗 → 重过本 gate（用户追认）

## 五、待用户晨间追认清单（按影响排序）

1. **全部 AI 代行决策**（本 pack 第二节 14 项 + prd.md C1-C17 + explore.md D1-D7 + workflow-state 两段裁决记录）
2. Stage 3 熔断代行裁决（FriendTagMapper 前移 T020，方案①）
3. Stage 4 三项裁决（imageUrls 契约/校验顺序/容差分母）
4. design §12 遗留项（发帖不做 requestId 幂等依据/mock postCount 扩展/上传频控拒绝等，均有留痕）
5. 环境：本地 MySQL 5.7.44 替代 PRD 8.0（DDL 双兼容，无功能影响）

## 六、用户结论

| 决策项 | 结论 | 备注 |
|--------|------|------|
| 全部冻结基线（第二节 14 项） | AI 代行：确认 | 依据目标指令授权；晨间可整体否决或逐项否决 |
| 阶段六入口 | AI 代行：放行 | 冻结生效，进入编码+测试红绿循环 |
